<nav class="navbar navbar-expand-lg navbar-dark bg-success shadow-lg">
  <div class="container">
    <a class="navbar-brand" href="/">GanjaWebshop<img src="/images/header-logo.svg" class="header-logo" alt=""></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav link-active">
        <% pages.forEach(page => { %>
        <% if(page.slug == "home") { %>
        <li class="nav-item"><a class="nav-link" href="/"><%= page.title %></a></li>
        <% } else { %>
        <li class="nav-item"><a class="nav-link" href="/<%= page.slug %>"><%= page.title %></a></li>
        <% } %>
        <% })%>
      </ul>
      <ul class="navbar-nav left-nav ms-auto d-flex align-items-center">
        <% if(user) { %>
          <li class="nav-item"><a class="nav-link" id="nav-link-logout" href="#">Logout</a></li>
          <li class="nav-item">
            <a class="nav-link d-flex align-items-center" href="/my-account">
              <img src="/images/users/<%= user.photo %>" class="bd-placeholder-img rounded-circle header-img" alt="<%= user.name %>">
              <% if(user.role === 'admin'){ %>
                <span><%= user.role %></span>
              <% } else { %>
                <span><%= user.name.split(" ")[0] %></span>
              <% }%>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/checkout">Your Cart
              <% if(cart.items) { %>
                <%= cart.items.length %>
              <% } %>
            </a>
          </li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<div id="liveAlertPlaceholder" class="text-center"></div>
    