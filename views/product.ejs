<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('layouts/head'); %>
    </head>
    <body class="container">

    <header>
        <%- include('layouts/header'); %>
    </header>

    <div class="container px-2 py-1">
        <div class="row flex-lg-row-reverse align-items-start g-6 py-2">
            <div class="col-10 col-sm-8 col-lg-6">
                <img src="/images/ganjas/<%= product.coverImage %>" class="d-block mx-auto img-fluid" alt="..." loading="lazy">
            </div>
            <div class="col-lg-6 text-secondary-emphasis">
                <h3 class="display-5 fw-bold lh-1 mb-3 font-monospace fs-2"><%= product.name %></h3>
                <img src="/images/thc-symbol.jpg" class="thc-symbol mb-1 " alt="">
                <p class="fw-semibold lh-sm">Category: <%= product.category%></p>
                <p class="fw-semibold lh-sm">Tested Date: <%= product.dateTested.toDateString() %></p>
                <p class="fw-semibold lh-sm">thca: <%= product.thca %>%</p>
                <p class="fw-semibold lh-sm">thc: <%= product.thc %>%</p>
                <p class="fw-semibold lh-sm">cbda: <%= product.cbda %>%</p>
                <p class="fw-semibold lh-sm">cbd: <%= product.cbd %>%</p>
                <p class="fw-semibold lh-sm">price: <%= product.price %>€</p>
                <p class="fw-semibold lh-sm">rating: <%= product.rating %></p>
                <a href="/reviews-by-product/<%= product.slug %>" role="button" class="btn btn-outline-light mb-1"><span class="fw-semibold text-secondary-emphasis">reviews</span><img src="/images/cannabis-logo-2.svg" alt="" width="57" height="42"></a>
                <p class="lh-base"><%= product.summary %></p>
                <% if(!user) { %>
                    <div class="flex-shrink-1 my-2">
                        <a href="/login" role="button" class="btn btn-success text-uppercase">login to purchase or add review</a>
                    </div>
                <% } else { %>
                    <div class="flex-shrink-1 my-2">
                        <a href="/add-to-cart/<%= product.id %>" role="button" class="btn btn-outline-success flex-shrink-0 text-uppercase">add to cart</a>
                    </div>
                    <div class="container px-2 py-2">
                        <form class="form-user-review">
                            <img class="mb-4" src="/images/cannabis-logo-2.svg" alt="" width="72" height="57">
                            <h4 class="mb-3">Add review</h4>
                            <div class="mb-3">
                                <label for="rating" class="form-label">Add rating</label>
                                <input type="number" class="form-control" id="rating">
                            </div>
                            <div class="mb-3">
                                <label for="content" class="form-label">Add Review</label>
                                <textarea class="form-control" id="content" rows="3"></textarea>
                            </div>
                            <input type="hidden" value="<%= product.id %>" id="hidden-id">
                            <button type="submit" class="btn btn-outline-success btn-lg px-4 me-md-2">Save</button>
                        </form>
                    </div>   
                <% }%>
            </div>
        </div>
    </div>

    <footer>
        <%- include('layouts/footer'); %>
    </footer>

    </body>
</html>










